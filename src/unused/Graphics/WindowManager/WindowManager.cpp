/*#include "processes/process.h"
#include "processes/processmanager.h"
#include "window.h"
#include <list>
#include <iostream>

using namespace std;
using std::list;*/

/*list<WindowManager::Window> windows = {};

WindowManager::Window CreateBaseWindow(char* WindowTitle, int x, int y, int width, int height) {
    WindowManager::Window window;

    window.Initialise(windows.size(), WindowTitle, x, y, width, height);

    windows.push_back(window);
}

WindowManager::Window CreateBaseWindowWithProcess(char* WindowTitle, int x, int y, int width, int height, int PID) {
    WindowManager::Window window;

    window.Initialise(windows.size(), WindowTitle, x, y, width, height);
    window.AssignProcess(PID);

    windows.push_back(window);
}*/